# Screenshots for README

Images referenced by the main `README.md`:

| File | What it shows |
|---|---|
| `home.png` | HomePage with a recent transcription card + 12-hour activity graph |

Recommended resolution: 1200-1600px wide, Windows light theme. Use the
`scripts/SeedDemoData` utility to populate the DB with realistic content
before capturing.
