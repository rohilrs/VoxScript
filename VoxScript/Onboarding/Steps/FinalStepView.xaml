<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VoxScript.Onboarding.Steps.FinalStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="TeaserCardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{StaticResource BrandMutedBrush}" />
            <Setter Property="Background" Value="#FFFEFB" />
            <Setter Property="Padding" Value="14,10,14,10" />
        </Style>
    </UserControl.Resources>
    <StackPanel MaxWidth="540" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
        <TextBlock Text="You're all set"
                   FontFamily="Georgia" FontSize="24" FontWeight="SemiBold" />
        <TextBlock Text="Try dictating anywhere — transcripts get pasted right where your cursor is. A few things you might want to set up next:"
                   FontSize="12" Foreground="{StaticResource BrandMutedBrush}"
                   TextWrapping="Wrap" Margin="0,0,0,8" />

        <Border Style="{StaticResource TeaserCardStyle}">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="✨" FontSize="20" VerticalAlignment="Center" />
                <StackPanel>
                    <TextBlock Text="AI Enhancement" FontWeight="SemiBold" />
                    <TextBlock Text="Clean up filler words, adjust tone, or reformat with an LLM."
                               FontSize="11" Foreground="{StaticResource BrandMutedBrush}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource TeaserCardStyle}">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="🎯" FontSize="20" VerticalAlignment="Center" />
                <StackPanel>
                    <TextBlock Text="Context modes" FontWeight="SemiBold" />
                    <TextBlock Text="Different tones for Slack vs Email vs personal messages."
                               FontSize="11" Foreground="{StaticResource BrandMutedBrush}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource TeaserCardStyle}">
            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="📖" FontSize="20" VerticalAlignment="Center" />
                <StackPanel>
                    <TextBlock Text="Dictionary &amp; Expansions" FontWeight="SemiBold" />
                    <TextBlock Text="Custom vocabulary, corrections, and text shortcuts."
                               FontSize="11" Foreground="{StaticResource BrandMutedBrush}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
