<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="VoxScript.Onboarding.Steps.WelcomeStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="14" MaxWidth="480">
        <TextBlock Text="VoxScript"
                   FontFamily="Georgia"
                   FontSize="48"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource BrandPrimaryBrush}"
                   HorizontalAlignment="Center" />
        <TextBlock Text="Local, private voice-to-text for Windows"
                   FontStyle="Italic"
                   FontSize="14"
                   Foreground="{StaticResource BrandMutedBrush}"
                   HorizontalAlignment="Center" />
        <TextBlock Text="Let's get you set up in about a minute."
                   FontSize="14"
                   HorizontalAlignment="Center"
                   Margin="0,12,0,0" />
        <StackPanel Spacing="4" Margin="0,12,0,0">
            <TextBlock Text="🎙️  Pick your mic and test the level" FontSize="13" />
            <TextBlock Text="💾  Download a transcription model (~142 MB)" FontSize="13" />
            <TextBlock Text="⌨️  Learn your dictation shortcuts" FontSize="13" />
            <TextBlock Text="✨  Record your first clip" FontSize="13" />
        </StackPanel>
    </StackPanel>
</UserControl>
