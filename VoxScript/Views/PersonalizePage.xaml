<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VoxScript.Views.PersonalizePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BrandBackgroundBrush}"
    Loaded="Page_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel HorizontalAlignment="Stretch"
                    Padding="60,40" Spacing="24">

            <!-- Page Title -->
            <TextBlock Text="Personalize"
                       FontFamily="Georgia" FontSize="40"
                       Foreground="{StaticResource BrandForegroundBrush}" />

            <TextBlock Text="Configure enhancement styles per app. Transcriptions are only enhanced when a context mode matches."
                       FontSize="15"
                       Foreground="{StaticResource BrandMutedBrush}"
                       Margin="0,-12,0,0" />

            <!-- Tab bar -->
            <StackPanel x:Name="TabBar" Orientation="Horizontal" Spacing="0" />

            <!-- Tab content -->
            <StackPanel x:Name="TabContent" Spacing="24" />

            <Border Height="20" />
        </StackPanel>
    </ScrollViewer>
</Page>
