<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VoxScript.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BrandBackgroundBrush}"
    KeyDown="Page_KeyDown"
    KeyUp="Page_KeyUp">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel HorizontalAlignment="Stretch"
                    Padding="60,40" Spacing="24">

            <!-- Page Title -->
            <TextBlock Text="Settings"
                       FontFamily="Georgia" FontSize="40"
                       Foreground="{StaticResource BrandForegroundBrush}" />

            <!-- Card 1: Keybinds -->
            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="16" Padding="28"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE92E;" FontSize="20"
                                      Foreground="{StaticResource BrandPrimaryBrush}" />
                            <TextBlock Text="KEYBINDS" FontSize="12" FontWeight="Medium"
                                       CharacterSpacing="120"
                                       Foreground="{StaticResource BrandMutedBrush}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                        <Button Content="Reset to defaults"
                                HorizontalAlignment="Right"
                                Click="ResetKeybindsButton_Click"
                                Background="Transparent"
                                Foreground="{StaticResource BrandMutedBrush}"
                                FontSize="13" Padding="8,4" />
                    </Grid>

                    <!-- Hold to talk -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Hold to talk"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Hold keys to record, release to stop"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                x:Name="HoldHotkeyButton"
                                Content="{x:Bind ViewModel.HoldHotkeyDisplay, Mode=OneWay}"
                                Click="HoldHotkeyButton_Click"
                                Background="{StaticResource BrandBackgroundBrush}"
                                Foreground="{StaticResource BrandForegroundBrush}"
                                FontFamily="Consolas" FontSize="14"
                                CornerRadius="8" Padding="16,8"
                                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                                BorderThickness="1"
                                MinWidth="160" MinHeight="38"
                                HorizontalContentAlignment="Center" />
                    </Grid>

                    <!-- Toggle talk -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Toggle talk"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Press to start, press again to stop"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                x:Name="ToggleHotkeyButton"
                                Content="{x:Bind ViewModel.ToggleHotkeyDisplay, Mode=OneWay}"
                                Click="ToggleHotkeyButton_Click"
                                Background="{StaticResource BrandBackgroundBrush}"
                                Foreground="{StaticResource BrandForegroundBrush}"
                                FontFamily="Consolas" FontSize="14"
                                CornerRadius="8" Padding="16,8"
                                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                                BorderThickness="1"
                                MinWidth="160" MinHeight="38"
                                HorizontalContentAlignment="Center" />
                    </Grid>

                    <!-- Paste last transcript -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Paste last transcript"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Paste your most recent dictation"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                x:Name="PasteLastHotkeyButton"
                                Content="{x:Bind ViewModel.PasteLastHotkeyDisplay, Mode=OneWay}"
                                Click="PasteLastHotkeyButton_Click"
                                Background="{StaticResource BrandBackgroundBrush}"
                                Foreground="{StaticResource BrandForegroundBrush}"
                                FontFamily="Consolas" FontSize="14"
                                CornerRadius="8" Padding="16,8"
                                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                                BorderThickness="1"
                                MinWidth="160" MinHeight="38"
                                HorizontalContentAlignment="Center" />
                    </Grid>

                    <!-- Cancel recording -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Cancel recording"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Stop without transcribing"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                x:Name="CancelHotkeyButton"
                                Content="{x:Bind ViewModel.CancelHotkeyDisplay, Mode=OneWay}"
                                Click="CancelHotkeyButton_Click"
                                Background="{StaticResource BrandBackgroundBrush}"
                                Foreground="{StaticResource BrandForegroundBrush}"
                                FontFamily="Consolas" FontSize="14"
                                CornerRadius="8" Padding="16,8"
                                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                                BorderThickness="1"
                                MinWidth="160" MinHeight="38"
                                HorizontalContentAlignment="Center" />
                    </Grid>

                    <!-- Keybind recording hint bar -->
                    <TextBlock x:Name="KeybindHint"
                               Text="Press desired key combination  ·  Esc to cancel  ·  Backspace to clear"
                               FontSize="13"
                               Foreground="{StaticResource BrandMutedBrush}"
                               HorizontalAlignment="Center"
                               Visibility="{x:Bind ViewModel.IsRecordingKeybind, Mode=OneWay}" />
                </StackPanel>
            </Border>

            <!-- Card 2: Input -->
            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="16" Padding="28"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE720;" FontSize="20"
                                  Foreground="{StaticResource BrandPrimaryBrush}" />
                        <TextBlock Text="INPUT" FontSize="12" FontWeight="Medium"
                                   CharacterSpacing="120"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- Model -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Transcription model"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock x:Name="CurrentModelText"
                                       Text="{x:Bind ViewModel.CurrentModelDisplay, Mode=OneWay}"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                Content="Manage models"
                                Click="ManageModelsButton_Click"
                                Background="{StaticResource BrandBackgroundBrush}"
                                Foreground="{StaticResource BrandForegroundBrush}"
                                FontSize="13"
                                CornerRadius="8" Padding="16,8"
                                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                                BorderThickness="1"
                                VerticalAlignment="Center" />
                    </Grid>

                    <!-- Microphone -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Microphone"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Audio input device for recording"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{x:Bind ViewModel.AudioDevices}"
                                  SelectedItem="{x:Bind ViewModel.SelectedAudioDevice, Mode=TwoWay}"
                                  Width="200"
                                  VerticalAlignment="Center" />
                    </Grid>

                    <!-- Language -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Language"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Transcription language"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{x:Bind ViewModel.Languages}"
                                  SelectedItem="{x:Bind ViewModel.SelectedLanguage, Mode=TwoWay}"
                                  Width="200"
                                  VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Card: AI Enhancement -->
            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="16" Padding="28"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE945;" FontSize="20"
                                  Foreground="{StaticResource BrandPrimaryBrush}" />
                        <TextBlock Text="AI ENHANCEMENT" FontSize="12" FontWeight="Medium"
                                   CharacterSpacing="120"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- Enable toggle -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Enable AI enhancement"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Polish transcriptions with AI (grammar, punctuation, style)"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.AiEnhancementEnabled, Mode=TwoWay}"
                                      VerticalAlignment="Center" />
                    </Grid>

                    <!-- Provider -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Provider"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="{x:Bind ViewModel.AiStatusText, Mode=OneWay}"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{x:Bind ViewModel.AiProviders}"
                                  SelectedIndex="{x:Bind ViewModel.SelectedAiProviderIndex, Mode=TwoWay}"
                                  Width="200"
                                  VerticalAlignment="Center" />
                    </Grid>

                    <!-- Model name -->
                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Model"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="AI model name for enhancement"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <TextBox Grid.Column="1"
                                 Text="{x:Bind ViewModel.AiModelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Width="200" FontSize="13"
                                 VerticalAlignment="Center" />
                    </Grid>

                    <!-- Ollama endpoint (local only) -->
                    <Grid ColumnSpacing="32"
                          Visibility="{x:Bind ViewModel.IsLocalProvider, Mode=OneWay}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Ollama endpoint"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Local Ollama server URL"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <TextBox Grid.Column="1"
                                 Text="{x:Bind ViewModel.OllamaEndpoint, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Width="200" FontSize="13"
                                 VerticalAlignment="Center" />
                    </Grid>

                    <!-- API Key (cloud only) -->
                    <Grid ColumnSpacing="32"
                          Visibility="{x:Bind ViewModel.IsCloudProvider, Mode=OneWay}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="API key"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="{x:Bind ViewModel.ApiKeyDisplay, Mode=OneWay}"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                            <Button Content="Set key"
                                    Click="SetApiKeyButton_Click"
                                    Background="{StaticResource BrandBackgroundBrush}"
                                    Foreground="{StaticResource BrandForegroundBrush}"
                                    FontSize="13" CornerRadius="8" Padding="12,8"
                                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                                    BorderThickness="1" />
                            <Button Content="Clear"
                                    Click="ClearApiKeyButton_Click"
                                    Background="Transparent"
                                    Foreground="{StaticResource BrandMutedBrush}"
                                    FontSize="13" CornerRadius="8" Padding="8,8" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Card 3: App -->
            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="16" Padding="28"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE713;" FontSize="20"
                                  Foreground="{StaticResource BrandPrimaryBrush}" />
                        <TextBlock Text="APP" FontSize="12" FontWeight="Medium"
                                   CharacterSpacing="120"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Launch at login"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Start VoxScript when you sign in"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.LaunchAtLogin, Mode=TwoWay}"
                                      VerticalAlignment="Center" />
                    </Grid>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Recording indicator"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Show floating bar when dictating"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{x:Bind ViewModel.RecordingIndicatorModes}"
                                  SelectedIndex="{x:Bind ViewModel.RecordingIndicatorModeIndex, Mode=TwoWay}"
                                  Width="200"
                                  VerticalAlignment="Center" />
                    </Grid>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Minimize to tray"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Keep running in system tray when closed"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.MinimizeToTray, Mode=TwoWay}"
                                      VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Card 4: Sound -->
            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="16" Padding="28"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE767;" FontSize="20"
                                  Foreground="{StaticResource BrandPrimaryBrush}" />
                        <TextBlock Text="SOUND" FontSize="12" FontWeight="Medium"
                                   CharacterSpacing="120"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Dictation sounds"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Play audio cues when recording starts and stops"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.SoundEffectsEnabled, Mode=TwoWay}"
                                      VerticalAlignment="Center" />
                    </Grid>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Pause media while dictating"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Mute other audio during recording"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.PauseMediaWhileDictating, Mode=TwoWay}"
                                      IsEnabled="True"
                                      VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Card 5: Extras -->
            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="16" Padding="28"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE734;" FontSize="20"
                                  Foreground="{StaticResource BrandPrimaryBrush}" />
                        <TextBlock Text="EXTRAS" FontSize="12" FontWeight="Medium"
                                   CharacterSpacing="120"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Auto-add to dictionary"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Learn frequently used words automatically"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.AutoAddToDictionary, Mode=TwoWay}"
                                      VerticalAlignment="Center" />
                    </Grid>

                    <Grid ColumnSpacing="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Smart formatting"
                                       FontSize="15" Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock Text="Auto-format punctuation and capitalization"
                                       FontSize="13" Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" MinWidth="0"
                                      OnContent="" OffContent=""
                                      IsOn="{x:Bind ViewModel.SmartFormattingEnabled, Mode=TwoWay}"
                                      VerticalAlignment="Center" />
                    </Grid>
                </StackPanel>
            </Border>

            <Border Height="20" />
        </StackPanel>
    </ScrollViewer>
</Page>
