<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VoxScript.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource BrandBackgroundBrush}">

    <Grid Padding="32,24" ColumnSpacing="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="240" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="180" />
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.Row="0"
                Background="{StaticResource BrandCardBrush}"
                CornerRadius="12" Padding="20"
                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                BorderThickness="1"
                Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="Latest transcript"
                                   FontSize="12" FontWeight="Medium"
                                   CharacterSpacing="60"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock x:Name="TimestampText"
                                   FontSize="12"
                                   Foreground="{StaticResource BrandMutedBrush}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8"
                                HorizontalAlignment="Right">
                        <Button x:Name="CopyButton" Content="Copy"
                                Background="Transparent"
                                Foreground="{StaticResource BrandMutedBrush}"
                                Click="CopyButton_Click" />
                        <Button Content="View history ›"
                                Background="Transparent"
                                Foreground="{StaticResource BrandPrimaryBrush}"
                                Click="ViewHistoryButton_Click" />
                    </StackPanel>
                </Grid>

                <TextBlock x:Name="TranscriptText"
                           Grid.Row="1"
                           TextWrapping="Wrap"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Top"
                           FontSize="17"
                           LineHeight="26"
                           Foreground="{StaticResource BrandForegroundBrush}" />

                <TextBlock x:Name="EmptyText"
                           Grid.Row="1"
                           Text="No transcriptions yet — press Ctrl+Win to start."
                           FontSize="15"
                           Foreground="{StaticResource BrandMutedBrush}"
                           Visibility="Collapsed" />
            </Grid>
        </Border>

        <Border Grid.Column="0" Grid.Row="1"
                Background="{StaticResource BrandCardBrush}"
                CornerRadius="12" Padding="20"
                BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Last 12 hours"
                           FontSize="12" FontWeight="Medium"
                           CharacterSpacing="60"
                           Foreground="{StaticResource BrandMutedBrush}"
                           Margin="0,0,0,10" />

                <Grid x:Name="GraphHost" Grid.Row="1" SizeChanged="GraphHost_SizeChanged">
                    <!-- Baseline -->
                    <Rectangle Height="1"
                               VerticalAlignment="Bottom"
                               Fill="{StaticResource BrandPrimaryLightBrush}"
                               Opacity="0.6" />
                    <!-- Line + area fill drawn in code-behind -->
                </Grid>

                <!-- Axis labels evenly spaced via 3 equal columns: -12h, -8h, -4h at column lefts; "now" overlaps column 2 right -->
                <Grid Grid.Row="2" Margin="0,6,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="-12h"
                               FontSize="10"
                               HorizontalAlignment="Left"
                               Foreground="{StaticResource BrandMutedBrush}" />
                    <TextBlock Grid.Column="1" Text="-8h"
                               FontSize="10"
                               HorizontalAlignment="Left"
                               Foreground="{StaticResource BrandMutedBrush}" />
                    <TextBlock Grid.Column="2" Text="-4h"
                               FontSize="10"
                               HorizontalAlignment="Left"
                               Foreground="{StaticResource BrandMutedBrush}" />
                    <TextBlock Grid.Column="2" Text="now"
                               FontSize="10"
                               HorizontalAlignment="Right"
                               Foreground="{StaticResource BrandMutedBrush}" />
                </Grid>
            </Grid>
        </Border>

        <StackPanel Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                    Spacing="10">

            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="12" Padding="16"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="4">
                    <TextBlock Text="STATUS"
                               FontSize="10" FontWeight="Medium"
                               CharacterSpacing="120"
                               Foreground="{StaticResource BrandMutedBrush}" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Ellipse x:Name="OverallDot" Width="10" Height="10"
                                 VerticalAlignment="Center" />
                        <TextBlock x:Name="OverallLabel"
                                   FontSize="14" FontWeight="Medium"
                                   Foreground="{StaticResource BrandForegroundBrush}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="12" Padding="16"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="12">
                    <TextBlock Text="COMPONENTS"
                               FontSize="10" FontWeight="Medium"
                               CharacterSpacing="120"
                               Foreground="{StaticResource BrandMutedBrush}" />

                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Ellipse x:Name="ModelDot" Grid.Column="0"
                                 Width="8" Height="8"
                                 VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Spacing="1">
                            <TextBlock Text="Model" FontSize="13" FontWeight="Medium"
                                       Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock x:Name="ModelLabel" FontSize="11"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                    </Grid>

                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Ellipse x:Name="AiDot" Grid.Column="0"
                                 Width="8" Height="8"
                                 VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Spacing="1">
                            <TextBlock Text="AI Enhancement" FontSize="13" FontWeight="Medium"
                                       Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock x:Name="AiLabel" FontSize="11"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                    </Grid>

                    <Grid ColumnSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Ellipse x:Name="LlmDot" Grid.Column="0"
                                 Width="8" Height="8"
                                 VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1" Spacing="1">
                            <TextBlock Text="LLM Formatting" FontSize="13" FontWeight="Medium"
                                       Foreground="{StaticResource BrandForegroundBrush}" />
                            <TextBlock x:Name="LlmLabel" FontSize="11"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource BrandMutedBrush}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="12" Padding="16"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="4">
                    <TextBlock Text="TOTAL WORDS"
                               FontSize="10" FontWeight="Medium"
                               CharacterSpacing="120"
                               Foreground="{StaticResource BrandMutedBrush}" />
                    <TextBlock x:Name="TotalWordsText"
                               Text="0"
                               FontSize="28" FontWeight="Medium"
                               Foreground="{StaticResource BrandForegroundBrush}" />
                </StackPanel>
            </Border>

            <Border Background="{StaticResource BrandCardBrush}"
                    CornerRadius="12" Padding="16"
                    BorderBrush="{StaticResource BrandPrimaryLightBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="4">
                    <TextBlock Text="AVG WPM"
                               FontSize="10" FontWeight="Medium"
                               CharacterSpacing="120"
                               Foreground="{StaticResource BrandMutedBrush}" />
                    <TextBlock x:Name="AvgWpmText"
                               Text="—"
                               FontSize="28" FontWeight="Medium"
                               Foreground="{StaticResource BrandForegroundBrush}" />
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</Page>
